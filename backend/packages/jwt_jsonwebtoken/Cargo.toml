[package]
name = "todoapp_graphql-jwt-jsonwebtoken"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
doctest = false

[dependencies]
jsonwebtoken = "10.3.0"
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0.18"
todoapp_graphql-jwt-port = { path = "../jwt_port" }
